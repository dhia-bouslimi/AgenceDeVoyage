<form method="post" action="{{ path('commande_delete', {'id_com': commande.idCom}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.idCom) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>